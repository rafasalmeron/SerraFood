import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#fff',
  },
  total: {
    marginTop: 20,
    padding: 15,
    backgroundColor: '#eee',
  },
  totalText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
